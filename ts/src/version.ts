/** Parser version — used as part of cache keys and emitted in manifests. */
export const PARSER_VERSION = "0.1.0";

/** Schema version for exporter outputs (NDJSON line shape, OpenViking layout, OpenClaw bundle). */
export const SCHEMA_VERSION = "1";
